import 'package:flutter/material.dart';

class WheelPointer extends StatelessWidget {
  const WheelPointer({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/reward/pointer.png',
    );
  }
}
